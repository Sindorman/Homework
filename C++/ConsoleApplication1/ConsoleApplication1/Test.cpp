#include "stdafx.h"
#include <string>
#include <iostream>
using namespace std;
/*
int main() {
	int one;
	int two;
	for (int x = 0; x < 999999; x++) {
		one = rand();
		while (one == 0) {
			one = rand();
		}
		two = rand();
		while (two == 0) {
			two = rand();
		}
		int third = (one + two % one) * one / two - one;
		cout << third << endl;
	}
}
*/